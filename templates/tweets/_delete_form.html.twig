<form method="post" action="{{ path('app_tweets_delete', {'id': tweet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tweet.id) }}">
    <button class="btn">Delete</button>
</form>
